#Exercise 1 Recangle Area Calc

length = float(input("what is the Lenght ?"))
width = float(input("what is the Width ?"))
area = float( length * width)
print(f"The area => Length * Width = {length} * {width} = {area}cm")   